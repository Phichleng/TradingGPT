// Phase 2 deliverable: live analysis dashboard + report viewer + journal UI.
// Subscribes to /v1/stream (WS) for analysis_ready events and renders
// AnalysisReport cards with Lightweight Charts overlays.
export default function Dashboard() {
  return <main>TradingGPT — analysis dashboard (stub)</main>;
}
